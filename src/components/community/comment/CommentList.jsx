import styled from 'styled-components';

const CommentListWrapper = styled.div`
  margin-top: 30px;
`;

const CommentList = () => {
    return (
        <CommentListWrapper>
            
        </CommentListWrapper>
    );
};

export default CommentList;